---
- name: "Helper — Force-delete user {{ cleanup_user_name }}"
  dellemc.objectscale.iam_user:
    user_name: "{{ cleanup_user_name }}"
    namespace_name: "{{ objectscale_namespace }}"
    state: absent
    force_delete: true
  failed_when: false
